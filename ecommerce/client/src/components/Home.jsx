import React from "react";
import { Container } from "react-bootstrap";

const Home = () => {
    return (
        <Container className="mt-5">
            <h1>Welcome Home</h1>
        </Container>
    );
};

export default Home;
